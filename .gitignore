# secrets — 로컬 테스트용 키, 배포 시엔 Streamlit Cloud secrets에 별도 등록
# 하위 폴더(예: gibong_proofreader/.streamlit/secrets.toml)에도 적용되도록 ** 사용
**/.streamlit/secrets.toml

# Python
__pycache__/
*.pyc
.pytest_cache/

# OS
.DS_Store
Thumbs.db

# 로컬 실행 로그 (공인 IP 등 노출될 수 있어 커밋 대상 아님)
*_log.txt
streamlit_edit_log.txt
